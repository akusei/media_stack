---
stack:
  name: radarr
  ensure_running:
    - radarr
  compose_directory: "{{ common_install_root }}/radarr"

targets:
  - src: install
    dest: "{{ common_install_root }}/radarr"
  - dest: "{{ common_data_root }}/radarr"

users:
  - user: "{{ common_downloads_user }}"
    group: "{{ common_downloads_group }}"

permissions:
  - owner: root
    group: root
    mode: '0440'
    dir_mode: '0550'
    paths:
      - install
      - install/*

  - owner: "{{ common_downloads_user }}"
    group: "{{ common_downloads_group }}"
    mode: '0440'
    dir_mode: '0750'
    paths:
      - "{{ common_data_root }}/radarr"
